import "@testing-library/jest-dom/vitest";

// Node < 18 需要 polyfill（Actions 默认 Node 20 一般不需要）
// import { fetch, Response, Request, Headers } from "undici";
// Object.assign(globalThis, { fetch, Response, Request, Headers });
